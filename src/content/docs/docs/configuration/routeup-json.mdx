---
title: routeup.json
description: Standalone config file for non-JavaScript projects.
---

> TODO: write the routeup.json reference.

Outline:

- Schema: `name` + `port` (minimal).
- Future fields (path-limited expose, etc.).
- Discovery rule: current working directory only, no walk-up in v1.

Source: `PLAN.md` Config section.
