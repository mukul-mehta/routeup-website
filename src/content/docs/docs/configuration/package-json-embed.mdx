---
title: package.json embed
description: Configure routeup inside an existing package.json under the `routeup` key.
---

> TODO: write the package.json embed reference.

Outline:

- Schema: same shape as `routeup.json` under `package.json` `routeup` key.
- Precedence: `routeup.json` wins if both exist in the same directory.
- Why this exists: zero-friction adoption for JS projects.

Source: `PLAN.md` Config section.
