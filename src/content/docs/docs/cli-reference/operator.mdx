---
title: Operator commands
description: Server-side commands for running a routeup deployment and minting tokens.
---

> TODO: document the operator-only commands.

Outline:

- `routeup server --domain <d>` — run the public server.
- `routeup token create <name> --allow <pattern>`.
- `routeup token list`.
- `routeup token revoke <token-id>`.
- These are hidden from `routeup --help` by default.

Source: `PLAN.md` Operator-only commands; Tokens.
