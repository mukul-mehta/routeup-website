---
title: routeup setup
description: One-time install of the local CA, certificates, agent autostart, and optional server credentials.
---

> TODO: document `routeup setup`.

Outline:

- What it mutates (trust store, certs, autostart unit).
- `--server` and `--token` for public configuration.
- Idempotent re-run behavior.

Source: `PLAN.md` Setup Model.
