---
title: routeup serve
description: Register a local route and reverse-proxy it to a local port.
---

> TODO: document `routeup serve`.

Outline:

- `routeup serve` (uses config).
- `routeup serve <name>` (bare name + project scope).
- `routeup serve <dotted.name>` (literal route).
- `--port`, `--expose`, `--dry-run` flags.

Source: `PLAN.md` User Experience.
