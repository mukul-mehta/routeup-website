---
title: routeup
description: The bare routeup command runs in Portless-style script-runner mode.
---

> TODO: document the bare command — config-driven script runner.

Outline:

- Run with no args inside a directory that has `routeup.json` or a `package.json` `routeup` block.
- Infers local vs local+expose from config.
- Injects `PORT`, `HOST`, `ROUTEUP_URL`, `ROUTEUP_LOCAL_URL` into the child process.

Source: `PLAN.md` User Experience; `docs/MILESTONES.md` Phase 9.
