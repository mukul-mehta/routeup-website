---
title: routeup expose
description: Make a route public — retrofit public exposure onto a route that's being served locally.
---

> TODO: document `routeup expose`.

Outline:

- Relationship to `serve --expose` (one-shot) vs standalone `expose` (retrofit).
- `--random` flag.
- `--server` and `--token` flags / env vars.

Source: `PLAN.md` Exposure Model.
