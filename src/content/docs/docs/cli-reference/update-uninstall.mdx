---
title: routeup update / uninstall
description: Lifecycle commands for upgrading and fully removing routeup.
---

> TODO: document update + uninstall.

Outline:

- `routeup update` — channel detection (Homebrew vs direct binary).
- `routeup uninstall` — tears down agent, autostart, CA, certs, state dir.
- Must work even when the binary is being replaced.

Source: `PLAN.md` Distribution And Lifecycle.
