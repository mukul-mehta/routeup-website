---
title: Running the server
description: Run your own routeup server on a custom domain.
---

> TODO: write the server-run guide.

Outline:

- `routeup server --domain <your.domain>`.
- Required config (DNS, TLS, reserved-subdomain list).
- Recommended deployment shape (single VM is fine for v1).
- Operational concerns: logs, restarts, backups of the SQLite token store.
