---
title: FAQ
description: Common questions about routeup.
---

> TODO: populate with real questions once users start asking them.

Seed questions:

- Why `.localhost` instead of `.test` or `/etc/hosts` entries?
- Why HTTPS for local development?
- Does routeup phone home? (No — zero telemetry.)
- Can I use a custom domain instead of `routeup.dev`?
- Windows support? (Not in v1.)
- What happens to my exposed routes when my laptop sleeps?
