---
title: Frontend + API behind one route
description: Serve a dev frontend at /  and an API at /api/* behind a single route.
---

> TODO: write the path-proxy recipe.

Outline:

- The single-origin pattern (no CORS, simpler cookies).
- `/` → dev server (Vite, Next).
- `/api/*` → API backend.
- Configuration in `routeup.json` / `package.json` embed.

Source: `docs/MILESTONES.md` Phase 8.
